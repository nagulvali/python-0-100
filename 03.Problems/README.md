# 🐍 Python Interview Problems (Beginner + Intermediate)

## ✅ Beginner Level

### Strings
1. Reverse a string
2. Check if a string is a palindrome
3. Count vowels and consonants in a string
4. Convert a string to title case without using `.title()`
5. Remove all whitespace from a string
6. Count frequency of each character
7. Remove duplicate characters
8. Find first non-repeating character
9. Replace a word in a string with another
10. Check if two strings are anagrams

### Lists
11. Find the maximum and minimum in a list
12. Find the second largest number
13. Remove duplicates from a list
14. Reverse a list without using `.reverse()`
15. Sum all elements of a list
16. Sort a list without using `sorted()`
17. Merge two lists
18. Remove all odd numbers from a list
19. Find common elements between two lists
20. Count the occurrences of each element in a list

### Numbers
21. Check if a number is even or odd
22. Check if a number is prime
23. Find the factorial of a number (iterative)
24. Find the sum of digits of a number
25. Generate the first N Fibonacci numbers
26. Check if a number is an Armstrong number
27. Reverse the digits of an integer
28. Find the GCD and LCM of two numbers
29. Convert decimal to binary
30. Find all prime numbers in a range

---

## 🧠 Intermediate Level

### Strings
31. Longest word in a sentence
32. Find all substrings of a string
33. Check if one string is rotation of another
34. Compress a string (e.g., `aaabb → a3b2`)
35. Remove all characters except alphabets
36. Find the most frequent character
37. Capitalize the first letter of every word (without `.title()`)
38. Find all palindromic substrings
39. Check if a string is a pangram
40. Split a string into equal-sized chunks

### Lists
41. Flatten a nested list
42. Rotate a list left/right by k steps
43. Move all zeroes to the end of a list
44. Pair elements from two lists into tuples
45. Find the difference between two lists
46. Create a list of squares for all even numbers
47. Remove elements from one list present in another
48. Chunk a list into sublists of size `n`
49. Count how many times an element appears consecutively
50. Replace every element with the product of all others (no division)

### Dictionaries / Tuples / Sets
51. Swap keys and values in a dictionary
52. Merge two dictionaries
53. Count word frequency in a sentence
54. Sort dictionary by values
55. Convert a list of tuples to a dictionary
56. Remove duplicates using sets
57. Find the intersection and union of two sets
58. Check if two dictionaries are equal
59. Group items by their first letter using a dictionary
60. Convert two lists into a dictionary (zip)

### Miscellaneous Logic Problems
61. Check if a list is sorted
62. Implement binary search on a list
63. Generate a pattern (like triangle of stars)
64. Print multiplication table of a number
65. Validate a password with given rules (e.g., min length, contains number)
66. Convert a string to integer without using `int()`
67. Count digits, letters, symbols in a string
68. Find all duplicates in a list
69. Implement basic calculator (add/sub/mul/div)
70. Count number of words in a sentence  